# 移除多語系殘留資料 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 移除 `index.html` 中所有 en / ja 字串與語系切換機制，畫面顯示結果完全不變。

**Architecture:** 先用 Node 腳本把資料區塊從原始碼抽出來 `eval`，產生一份「所有中文字串」的正規化 JSON 快照當基準線。接著分批壓平語系物件，每批改完重跑快照與基準線 diff——只要有任何一個中文字串被誤刪或留錯語言，diff 就會抓到。最後移除 `LANG` 機制並以瀏覽器確認。

**Tech Stack:** 單檔 vanilla JS（`index.html`，無建置流程、無測試框架）、Node（僅用於驗證腳本）。

**Spec:** `docs/superpowers/specs/2026-08-05-remove-i18n-design.md`

**前置條件：** `docs/superpowers/plans/2026-08-05-itinerary-auto-date.md` 已全部完成並 commit。

**路徑簡寫：** 以下 `$SP` 代表 `/private/tmp/claude-501/-Users-raychang-tirp/29d6540d-5614-4f2b-9e56-f7a25a26c8e2/scratchpad`

---

### Task 1: 建立快照工具與基準線

**Files:**
- Create: `$SP/i18n-snapshot.js`
- Create: `$SP/baseline.json`（產出物）

- [ ] **Step 1: 寫快照腳本**

建立 `$SP/i18n-snapshot.js`：

```js
const fs = require('fs');

const SRC = '/Users/raychang/tirp/index.html';
const src = fs.readFileSync(SRC, 'utf8');

// 抽出資料區塊（const WD= 起，到 const SEED_TOTAL= 之前）
const start = src.indexOf('const WD=');
const end = src.indexOf('const SEED_TOTAL=');
if (start < 0 || end < 0 || end <= start) {
  console.error('FAIL: 找不到資料區塊邊界（const WD= / const SEED_TOTAL=）');
  process.exit(1);
}
const block = src.slice(start, end);

// eval 後把需要的常數回傳出來（const 在 eval 內是區塊作用域，必須用尾端運算式取值）
const D = eval(block + '\n;({WD,UI,STAY,DEFAULT_DAYS,TODOS,CATS,TRANSPORT_EST,L_E5489,L_HWB})');

// 語系物件與純字串都解析成中文
const s = o => o == null ? '' : (typeof o === 'string' ? o : (o.zh || ''));
const arr = o => Array.isArray(o) ? o : ((o && o.zh) || []);

const LANG_KEYS = ['regions', 'route', 'regionKey'];

const snap = {
  WD: arr(D.WD),
  UI_regions: arr(D.UI.regions),
  UI_route: arr(D.UI.route),
  UI_regionKey: arr(D.UI.regionKey),
  UI: Object.fromEntries(
    Object.keys(D.UI).filter(k => !LANG_KEYS.includes(k)).sort().map(k => [k, s(D.UI[k])])
  ),
  L_E5489: s(D.L_E5489),
  L_HWB: s(D.L_HWB),
  STAY: Object.fromEntries(
    Object.keys(D.STAY).sort().map(k => [k, { name: s(D.STAY[k]), url: D.STAY[k].url || '' }])
  ),
  DEFAULT_DAYS: D.DEFAULT_DAYS.map(d => ({
    date: d.date, r: d.r, leaf: !!d.leaf,
    dest: s(d.dest), trans: s(d.trans), note: s(d.note),
    url: d.url || '', ulabel: s(d.ulabel),
    stay: s(d.stay), stayUrl: (d.stay && d.stay.url) || '',
  })),
  TODOS: D.TODOS.map(x => ({
    id: x.id, due: x.due, time: x.time, url: x.url,
    title: s(x.title), forDay: s(x.forDay),
  })),
  CATS: D.CATS.map(c => ({ id: c.id, color: c.color, name: s(c.name) })),
  TRANSPORT_EST: D.TRANSPORT_EST.map(x => ({ date: x.date, jpy: x.jpy, note: s(x.note) })),
};

process.stdout.write(JSON.stringify(snap, null, 1) + '\n');
```

- [ ] **Step 2: 產生基準線並目視確認內容正確**

Run:
```bash
node $SP/i18n-snapshot.js > $SP/baseline.json && wc -l $SP/baseline.json && head -30 $SP/baseline.json
```
Expected: 順利產出，`WD` 是 `["一","二","三","四","五","六","日"]`，接著是中文的 `UI_regions`、`UI_route` 等。若出現任何英文或日文字串代表 `s()` / `arr()` 取值有誤，必須先修好腳本再繼續。

- [ ] **Step 3: 確認基準線裡沒有英文/日文殘留**

Run:
```bash
grep -nE '"[A-Za-z][A-Za-z ]{6,}"' $SP/baseline.json | grep -v '"url"\|"id"\|"color"\|"date"\|http\|var(--\|Tabino\|KLOOK\|JR \|VIP\|INN' | head
```
Expected: 無輸出（或僅剩明顯屬於中文文案一部分的英數字，例如飯店名 `Tabino Hotel Satellite（高山）`）。有非預期的整句英文就是快照取到了錯誤語言。

- [ ] **Step 4: Commit 快照腳本**

快照腳本放在 scratchpad，不進 repo，無需 commit。確認 `git status --short` 是乾淨的即可。

Run:
```bash
cd /Users/raychang/tirp && git status --short
```
Expected: 無輸出。

---

### Task 2: 壓平 `UI` 字典

**Files:**
- Modify: `index.html`（`const UI={` 到對應的 `};`）

- [ ] **Step 1: 壓平所有 `{zh,en,ja}` 項目**

把 `UI` 內每個 `key:{zh:'中文',en:'…',ja:'…'}` 改成 `key:'中文'`。

範例（前）：
```js
 lblTwd:{zh:'台幣',en:'TWD',ja:'台湾ドル'},
```
（後）：
```js
 lblTwd:'台幣',
```

`regions` / `route` / `regionKey` 三個是陣列，改成只留中文那個陣列。

範例（前）：
```js
 regions:{zh:['名古屋','北陸','信州','飛驒','其他'],en:['Nagoya','Hokuriku','Shinshu','Hida','Other'],ja:['名古屋','北陸','信州','飛騨','その他']},
```
（後）：
```js
 regions:['名古屋','北陸','信州','飛驒','其他'],
```

- [ ] **Step 2: 快照比對**

Run:
```bash
node $SP/i18n-snapshot.js > $SP/after.json && diff $SP/baseline.json $SP/after.json && echo "SNAPSHOT OK"
```
Expected: `SNAPSHOT OK`（無 diff 輸出）。有 diff 就是有字串被改錯或漏刪，依 diff 內容修正後重跑。

- [ ] **Step 3: Commit**

```bash
cd /Users/raychang/tirp
git add index.html
git commit -m "壓平 UI 字典，移除 en/ja"
```

---

### Task 3: 壓平 `WD`、`L_E5489`、`L_HWB`、`STAY`

**Files:**
- Modify: `index.html`

- [ ] **Step 1: `WD` 改成純陣列**

（前）
```js
const WD={zh:['一','二','三','四','五','六','日'],en:['Mon','Tue','Wed','Thu','Fri','Sat','Sun'],ja:['月','火','水','木','金','土','日']};
```
（後）
```js
const WD=['一','二','三','四','五','六','日'];
```

- [ ] **Step 2: `L_E5489` / `L_HWB` 改成字串**

（前）
```js
const L_E5489={zh:'JR e5489 訂票',en:'Book on JR e5489',ja:'JR e5489 で予約'};
const L_HWB={zh:'高速巴士 訂票',en:'Book highway bus',ja:'高速バス予約'};
```
（後）
```js
const L_E5489='JR e5489 訂票';
const L_HWB='高速巴士 訂票';
```

- [ ] **Step 3: `STAY` 只刪 en/ja，保留物件形狀**

**這裡不能壓平成字串**——`url` 與語系鍵在同一個物件裡，壓平會弄丟住宿的 Google Maps 連結。

（前）
```js
const STAY={
 ngo:{zh:'名古屋花園皇宮飯店',en:'Nagoya Garden Palace Hotel',ja:'名古屋ガーデンパレス',url:'https://maps.app.goo.gl/kP9Ns6WdLcvb99FJA'},
 toy:{zh:'富山地鐵飯店',en:'Toyama Chitetsu Hotel',ja:'富山地鉄ホテル',url:'https://maps.app.goo.gl/VprwdcATF86Ttm9YA'},
 mat:{zh:'東橫 INN 松本站東口',en:'Toyoko Inn Matsumoto Sta. East',ja:'東横INN松本駅東口',url:'https://maps.app.goo.gl/RU9hxcBN87C51DPTA'},
 tak:{zh:'Tabino Hotel Satellite（高山）',en:'Tabino Hotel Satellite (Takayama)',ja:'Tabino Hotel Satellite（高山）',url:'https://maps.app.goo.gl/bDSbYodPWsvwXU3R9'},
 nm:{zh:'東橫 INN 名古屋丸之內',en:'Toyoko Inn Nagoya Marunouchi',ja:'東横INN名古屋丸の内',url:'https://maps.app.goo.gl/BgL2odEDoFHaqw1Y7'},
 home:{zh:'溫暖的家 ♡',en:'Home sweet home ♡',ja:'我が家 ♡'}
};
```
（後）
```js
const STAY={
 ngo:{zh:'名古屋花園皇宮飯店',url:'https://maps.app.goo.gl/kP9Ns6WdLcvb99FJA'},
 toy:{zh:'富山地鐵飯店',url:'https://maps.app.goo.gl/VprwdcATF86Ttm9YA'},
 mat:{zh:'東橫 INN 松本站東口',url:'https://maps.app.goo.gl/RU9hxcBN87C51DPTA'},
 tak:{zh:'Tabino Hotel Satellite（高山）',url:'https://maps.app.goo.gl/bDSbYodPWsvwXU3R9'},
 nm:{zh:'東橫 INN 名古屋丸之內',url:'https://maps.app.goo.gl/BgL2odEDoFHaqw1Y7'},
 home:{zh:'溫暖的家 ♡'}
};
```

- [ ] **Step 4: 改掉 `WD[LANG][wd]`**

在 `renderTimeline()` 中找到：
```js
${WD[LANG][wd]}
```
改成：
```js
${WD[wd]}
```

- [ ] **Step 5: 快照比對**

Run:
```bash
node $SP/i18n-snapshot.js > $SP/after.json && diff $SP/baseline.json $SP/after.json && echo "SNAPSHOT OK"
```
Expected: `SNAPSHOT OK`。特別確認 `STAY` 每一項的 `url` 都還在（diff 沒有 `"url": ""` 出現）。

- [ ] **Step 6: Commit**

```bash
cd /Users/raychang/tirp
git add index.html
git commit -m "壓平 WD、訂票標籤與住宿資料，移除 en/ja"
```

---

### Task 4: 壓平 `DEFAULT_DAYS`

**Files:**
- Modify: `index.html`（`const DEFAULT_DAYS=[` 到對應的 `];`）

- [ ] **Step 1: 壓平每一天的 `dest` / `trans` / `note` / `ulabel`**

把每個 `{zh:'中文',en:'…',ja:'…'}` 改成 `'中文'`。`stay:STAY.xxx` 保持不動（Task 3 已處理）。

範例（前）：
```js
 {n:3,date:'10/23',wd:4,r:'slate',stay:STAY.toy,
  dest:{zh:'雨晴海岸',en:'Amaharashi Coast',ja:'雨晴海岸'},
  trans:{zh:'上午: 07:30退房…',en:'…',ja:'…'},
  note:{zh:'9/23 訂票…',en:'…',ja:'…'},url:URL_E5489,ulabel:L_E5489},
```
（後）：
```js
 {n:3,date:'10/23',wd:4,r:'slate',stay:STAY.toy,
  dest:'雨晴海岸',
  trans:'上午: 07:30退房…',
  note:'9/23 訂票…',url:URL_E5489,ulabel:L_E5489},
```

`ulabel` 若是內嵌語系物件（例如 `ulabel:{zh:'立山黑部 官網',en:'…',ja:'…'}`）也一併壓平成字串；若是 `ulabel:L_E5489` 這種引用則不動。

- [ ] **Step 2: 快照比對**

Run:
```bash
node $SP/i18n-snapshot.js > $SP/after.json && diff $SP/baseline.json $SP/after.json && echo "SNAPSHOT OK"
```
Expected: `SNAPSHOT OK`。15 天的 `dest` / `trans` / `note` / `ulabel` 全部一字不差。

- [ ] **Step 3: Commit**

```bash
cd /Users/raychang/tirp
git add index.html
git commit -m "壓平 DEFAULT_DAYS，移除 en/ja"
```

---

### Task 5: 壓平 `TODOS`、`CATS`、`TRANSPORT_EST`

**Files:**
- Modify: `index.html`

- [ ] **Step 1: `TODOS` 的 `title` / `forDay`**

範例（前）：
```js
 {id:'tk',due:'2026-07-31',time:'09:00',url:'https://tateyama-kurobe-webservice.jp/AlpenTour/html/VW001W0010.html',
  title:{zh:'立山黑部 早鳥票（官網）',en:'Tateyama Kurobe early-bird (official)',ja:'立山黒部 早割（公式）'},
  forDay:{zh:'10/26 立山黑部橫越',en:'10/26 Tateyama traverse',ja:'10/26 立山黒部横断'}},
```
（後）：
```js
 {id:'tk',due:'2026-07-31',time:'09:00',url:'https://tateyama-kurobe-webservice.jp/AlpenTour/html/VW001W0010.html',
  title:'立山黑部 早鳥票（官網）',
  forDay:'10/26 立山黑部橫越'},
```

- [ ] **Step 2: `CATS` 的 `name`**

（前）
```js
 {id:'transport',color:'#5b7c91',name:{zh:'交通',en:'Transport',ja:'交通'}},
```
（後）
```js
 {id:'transport',color:'#5b7c91',name:'交通'},
```

其餘五項同樣處理：`lodging`→`'住宿'`、`food`→`'餐飲'`、`ticket`→`'門票・活動'`、`shopping`→`'購物'`、`other`→`'其他'`。

- [ ] **Step 3: `TRANSPORT_EST` 的 `note`**

（前）
```js
 {date:'2026-10-21',jpy:1340,note:{zh:'機場→名古屋 名鐵 μ-Sky',en:'Airport→Nagoya, Meitetsu μ-Sky',ja:'空港→名古屋 名鉄μ-SKY'}},
```
（後）
```js
 {date:'2026-10-21',jpy:1340,note:'機場→名古屋 名鐵 μ-Sky'},
```

17 筆全部處理。

- [ ] **Step 4: 快照比對**

Run:
```bash
node $SP/i18n-snapshot.js > $SP/after.json && diff $SP/baseline.json $SP/after.json && echo "SNAPSHOT OK"
```
Expected: `SNAPSHOT OK`。

- [ ] **Step 5: 確認資料區已無 en/ja**

Run:
```bash
cd /Users/raychang/tirp && grep -nE "en:'|en:\`|ja:'|ja:\`" index.html
```
Expected: 只剩 `cd()`（倒數文案）與 `seedSubHtml()`（預估說明）兩處，共 4 行左右。Task 6 處理。

- [ ] **Step 6: Commit**

```bash
cd /Users/raychang/tirp
git add index.html
git commit -m "壓平 TODOS、CATS、TRANSPORT_EST，移除 en/ja"
```

---

### Task 6: 移除語系機制

**Files:**
- Modify: `index.html`（`cd()`、`seedSubHtml()`、`t()`、`LANG`、`HTMLLANG`、`applyStatic()`、`openDayForm()` 的 regions 取值、init）

- [ ] **Step 1: `cd()` 的內嵌語系物件**

（前）
```js
function cd(n){
 const L={zh:{l:'剩 '+n+' 天',t:'明天',td:'今天！',o:'已過 '+(-n)+' 天'},en:{l:n+' days left',t:'Tomorrow',td:'Today!',o:n===-1?'1 day ago':(-n)+' days ago'},ja:{l:'残り '+n+' 日',t:'明日',td:'本日！',o:(-n)+'日経過'}}[LANG];
```
（後）
```js
function cd(n){
 const L={l:'剩 '+n+' 天',t:'明天',td:'今天！',o:'已過 '+(-n)+' 天'};
```

- [ ] **Step 2: `seedSubHtml()` 的內嵌語系物件**

（前）
```js
function seedSubHtml(twd){
 const T={zh:`依官網最新票價估算 17 段交通，合計約 <b>¥${fmt(SEED_TOTAL)}</b>（≈ NT$${twd}）。可一鍵填入下方記帳，再依實際金額修改或刪除。`,
 en:`Estimated from the latest official fares across 17 legs — about <b>¥${fmt(SEED_TOTAL)}</b> (≈ NT$${twd}). Add them to your log in one tap, then edit or delete to match actual costs.`,
 ja:`公式の最新運賃をもとに17区間を概算、合計 約<b>¥${fmt(SEED_TOTAL)}</b>（≈ NT$${twd}）。ワンタップで下の家計簿に追加、実額に合わせて編集・削除できます。`};
 return T[LANG];
}
```
（後）
```js
function seedSubHtml(twd){
 return `依官網最新票價估算 17 段交通，合計約 <b>¥${fmt(SEED_TOTAL)}</b>（≈ NT$${twd}）。可一鍵填入下方記帳，再依實際金額修改或刪除。`;
}
```

- [ ] **Step 3: 簡化 `t()`**

（前）
```js
const t=o=>o==null?'':(typeof o==='string'?o:(o[LANG]||o.zh||''));
```
（後）
```js
// Firebase 上的舊資料仍是 {zh,en,ja} 物件，因此保留讀 o.zh 的能力
const t=o=>o==null?'':(typeof o==='string'?o:(o.zh||''));
```

- [ ] **Step 4: 刪除 `LANG` 與 `HTMLLANG`**

刪掉這兩行：
```js
let LANG='zh';
```
```js
const HTMLLANG={zh:'zh-Hant',en:'en',ja:'ja'};
```

刪掉 init 裡的這一行：
```js
 LANG='zh';
```

- [ ] **Step 5: 改掉剩下的 `[LANG]` 取值**

`applyStatic()` 內：

（前）`document.documentElement.lang=HTMLLANG[LANG];`
（後）`document.documentElement.lang='zh-Hant';`

（前）`document.getElementById('route').innerHTML=UI.route[LANG].map((label,i)=>{`
（後）`document.getElementById('route').innerHTML=UI.route.map((label,i)=>{`

（前）`const rk=UI.regionKey[LANG],rc=[…];`
（後）`const rk=UI.regionKey,rc=[…];`

`openDayForm()` 內：

（前）`rsel.innerHTML=REGION_OPTS.map((o,k)=>`<option value="${o.key}">${UI.regions[LANG][k]}</option>`).join('');`
（後）`rsel.innerHTML=REGION_OPTS.map((o,k)=>`<option value="${o.key}">${UI.regions[k]}</option>`).join('');`

- [ ] **Step 6: 把 `/* ===== language ===== */` 註解改名**

（前）`/* ===== language ===== */`
（後）`/* ===== static text ===== */`

- [ ] **Step 7: 確認 en/ja 與 LANG 都清乾淨**

Run:
```bash
cd /Users/raychang/tirp && echo "en/ja 殘留：$(grep -cE "en:'|en:\`|ja:'|ja:\`" index.html)" && echo "LANG 殘留：" && grep -n "LANG" index.html
```
Expected: `en/ja 殘留：0`；`LANG` 只剩 `URL_E5489` 那一行網址中的 `?LANG=tc`（外部連結參數，保留）。

- [ ] **Step 8: 快照比對**

Run:
```bash
node $SP/i18n-snapshot.js > $SP/after.json && diff $SP/baseline.json $SP/after.json && echo "SNAPSHOT OK"
```
Expected: `SNAPSHOT OK`。

- [ ] **Step 9: 語法檢查**

Run:
```bash
cd /Users/raychang/tirp && sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > $SP/extracted.js && node --check $SP/extracted.js && echo "SYNTAX OK"
```
Expected: `SYNTAX OK`。

若 `index.html` 有多個 `<script>` 區塊使得抽取結果不是完整的一段程式（node 報出與本次改動無關的語法錯誤），略過這一步，改由 Task 7 Step 2 的瀏覽器 console 檢查把關。

- [ ] **Step 10: Commit**

```bash
cd /Users/raychang/tirp
git add index.html
git commit -m "移除 LANG 語系切換機制"
```

---

### Task 7: 瀏覽器驗證

**Files:** 無（純驗證）

- [ ] **Step 1: 啟動本機伺服器**

Run（背景執行）：
```bash
cd /Users/raychang/tirp && python3 -m http.server 8899
```

- [ ] **Step 2: 開頁面並確認 console 無錯誤**

開 `http://localhost:8899/`，打開開發者工具 console。

Expected: 無 JavaScript 錯誤（`is not defined`、`Cannot read properties of undefined` 等）。

- [ ] **Step 3: 行程頁比對**

Expected:
- 頁首路線列（名古屋 → 北陸 → 信州 → 飛驒 → 名古屋）與地區圖例正常
- 15 天的目的地、交通、住宿、備註全部是中文且內容不變
- 星期標籤正確
- **住宿名稱是可點擊的連結**（右上有 ↗），點下去會開 Google Maps——這是 `STAY.url` 有保住的關鍵驗證

- [ ] **Step 4: 記帳頁比對**

Expected:
- 六個分類名稱（交通／住宿／餐飲／門票・活動／購物／其他）正常
- 「依官網最新票價估算 17 段交通，合計約 ¥…（≈ NT$…）」這段說明文字正常顯示，金額有算出來
- 幣別標籤（日圓／台幣）正常

- [ ] **Step 5: 待辦頁比對**

Expected:
- 9 項待辦的標題與對應日期文字正常
- 倒數天數文字正常（「剩 N 天」／「明天」／「今天！」／「已過 N 天」其中之一，視今天日期而定）

- [ ] **Step 6: 編輯視窗比對**

進入編輯模式點任一天的 ✏️。

Expected: 地區下拉選單顯示中文（名古屋／北陸／信州／飛驒／其他），所有欄位標籤正常。

- [ ] **Step 7: 確認 Firebase 舊資料仍正常顯示**

這一步驗證 `t()` 的物件 fallback 還有效——資料庫裡的行程內容是 `{zh,en,ja}` 結構。

Expected: 若右上沒有「連不上資料庫」紅字，代表讀到了資料庫內容；此時行程頁若正常顯示中文，即代表 fallback 有效。若顯示空白，`t()` 的改動有問題，回頭檢查 Task 6 Step 3。

- [ ] **Step 8: 確認檔案瘦身幅度**

Run:
```bash
cd /Users/raychang/tirp && wc -l index.html && git diff --stat HEAD~5 -- index.html
```
Expected: 行數明顯減少（原 1004 行，預期減少約 150 行）。

- [ ] **Step 9: 確認狀態乾淨**

Run:
```bash
cd /Users/raychang/tirp && git status --short
```
Expected: 無輸出。

---

## 完成後

兩份計畫都執行完畢。若要部署，`.github/workflows/` 已設定 GitHub Actions，push 到 `main` 即可。
